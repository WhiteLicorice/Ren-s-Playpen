/* var feedback = document.getElementById("feedback"); */

feedback.addEventListener("submit", function(event) {
	document.getElementById("feedback").submit();
	
	//Prevents the usual submit event
	/* event.preventDefault();
	//Get values of the message and email fields
	var message = document.getElementById('message').value;
	var email = document.getElementById('email').value;
	//Log parsed values in console --- placeholder for backend script 
	console.log(message);
	console.log(email); */
	
})